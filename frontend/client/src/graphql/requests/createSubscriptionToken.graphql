mutation createSubscriptionToken($accountId: String!, $deviceGroupId: String!) {
  createDeviceGroupSubscription(
    accountId: $accountId
    groupId: $deviceGroupId
  ) {
    token
    expiresAt
  }
}
